spring:
  profile:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/adrielcardoso/EmailServiceConfigProperties.git/
          search-paths:
              - "EmailServiceConfigProperties/{application}/{profile}"